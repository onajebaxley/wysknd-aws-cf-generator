'use strict';

/**
 * Entry point for lambda templates
 */
const index = {
    /**
     * Reference to the template abstraction for Lambda functions
     */
    LambdaFunctionTemplate: require('./lambda-function-template')
};

module.exports = index;
